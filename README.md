# hackathon-task-frontend

Vite + React starter for weekly hackathons. Dev server proxies `/api` and `/health` to the Nest backend (`VITE_PROXY_TARGET`, default `http://127.0.0.1:5000`). Branch new work from `main`.
